# See https://www.dartlang.org/guides/libraries/private-files

# Files and directories created by pub
.dart_tool/
.packages
build/
# If you're building an application, you may want to check-in your pubspec.lock
pubspec.lock

# Directory created by dartdoc
# If you don't generate documentation locally you can remove this line.
doc/api/

# dotenv environment variables file
.env*
# El .example SÍ se versiona: contiene solo placeholders, no secretos
!.env.json.example

# Avoid committing generated Javascript files:
*.dart.js
# Produced by the --dump-info flag.
*.info.json
# When generated by dart2js. Don't specify *.js if your
# project includes source files written in JavaScript.
*.js
*.js_
*.js.deps
*.js.map
# Excepción: Service Worker de Firebase (no es código generado)
!web/firebase-messaging-sw.js

.flutter-plugins
.flutter-plugins-dependencies

# Variables de entorno secretas (ahora manejadas con .env.json)

# Scripts temporales
scratch/
supabase/.temp/

# Credenciales de Firebase (NUNCA subir al repositorio)
google-services.json
GoogleService-Info.plist
secrets/